# Configuration pour les tests
MOCK_AUTH0=true
BACKEND_TMPFILE_PERSISTENCE=3000
BACKEND_TOKEN_KEY=test-secret-key
BACKEND_TOKEN_USER=test@example.com
BACKEND_TOKEN_PASSWORD=test-password
BACKEND_TMP_MAX=300
BACKEND_TMP_DURATION=14400
BACKEND_TMP_WINDOW=86400
BACKEND_JOB_CONCURRENCY=1
BACKEND_CHUNK_CONCURRENCY=1
BACKEND_LOG_LEVEL=error
BACKEND_LOG_TIMER=false

# Configuration des fichiers de données
COMMUNES_JSON=data/communes.json
DISPOSABLE_MAIL=data/disposable-mail.txt
DB_JSON=data/userDB.json
WIKIDATA_LINKS=data/wikidata.json
PROOFS=data/proofs
JOBS=data/jobs

# Configuration SMTP pour les tests
SMTP_HOST=127.0.0.1
SMTP_PORT=587
SMTP_USER=test
SMTP_PWD=test
SMTP_TLS_SELFSIGNED=true

# Configuration Auth0 (mode mock)
AUTH0_DOMAIN=test.auth0.com
AUTH0_CLIENT_ID=test-client-id
AUTH0_CLIENT_SECRET=test-client-secret
AUTH0_AUDIENCE=test-audience

# Configuration Elasticsearch
ES_URL=localhost:9200
ES_INDEX=test-index

# Configuration Redis (pour les tests, on utilisera une instance locale)
REDIS_HOST=localhost
REDIS_PORT=6379