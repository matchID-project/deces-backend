{
  "name": "deces-backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build:routes": "mkdir -p src/routes && ./node_modules/.bin/tsoa routes",
    "build:swagger": "mkdir -p src/api && ./node_modules/.bin/tsoa swagger",
    "build:ts": "./node_modules/.bin/tsc --outDir dist",
    "build": "npm run build:routes && npm run build:swagger && npm run build:ts",
    "prestart": "npm run build",
    "start": "npm run build && node .",
    "watch-ts": "npm run build:routes && npm run build:swagger && ./node_modules/.bin/tsc --outDir dist -w",
    "watch-node": "nodemon .",
    "dev": "concurrently -k -p \"[{name}]\" -n \"TypeScript,Node\" -c \"cyan.bold,green.bold\" \"npm run watch-ts\" \"npm run watch-node\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Cristian Brokate",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "express": "^4.17.1",
    "swagger-ui-express": "^4.1.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.2",
    "@types/node": "^13.7.7",
    "@types/swagger-ui-express": "^4.1.1",
    "concurrently": "^5.1.0",
    "nodemon": "^2.0.2",
    "tslint": "^6.0.0",
    "tsoa": "^2.5.13",
    "typescript": "^3.7.5"
  }
}
